import 'package:flutter/material.dart';

final txtColr = Colors.black;
final pColr = Colors.white;
final trueCard = Colors.green;
final falseCard = Colors.red;

final kShadow = BoxShadow(
  color: Colors.blueGrey,
  blurRadius: 10,
  offset: Offset(0, 5),
);

final kGradient = LinearGradient(
    begin: Alignment.topRight,
    end: Alignment.bottomLeft,
    colors: [
      Color(0xFF6930c3),
      Color(0xFF7400b8),
    ]);
